// ficha es el objeto usado en el juego Ta-Te-Ti para jugar
var ficha={
    jugador,
    img,
    tiempo
}

// Cada uno de los objetos usados en el juego Pares
var carta={
    jugador,
    img,
    posicionPropia,
    posicionPar,
    activa
}

// Para personalizar cada juego
var jugador={
    jugador,
    nombre,
    color,
    ficha,
    activo,
    tiempo
}